export const dashboardBFFHost = "http://dashboard-bff-service.data-mesh:8080";

// uncomment this line for local debugging after first running up the dashboard server (e.g. make run)
//export const dashboardBFFHost = "http://localhost:8081";